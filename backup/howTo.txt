// click settings icon next to Project Settings in Firebase
// go to Service Accounts, create a new Secret Private Key, add it to main, and rename to appConfig.json
// npx -p node-firestore-import-export firestore-export -a appConfig.json -b backup.json